import { PlaceholderStylePipe } from './placeholder-style.pipe';

describe('PlaceholderStylePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceholderStylePipe();
    expect(pipe).toBeTruthy();
  });
});
